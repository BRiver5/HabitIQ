Generic single-database Alembic configuration for HabitIQ.

Create the initial migration after installing requirements:

    alembic revision --autogenerate -m "initial schema"
    alembic upgrade head
